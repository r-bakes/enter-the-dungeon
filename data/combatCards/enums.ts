export enum CombatCardTarget {
  ENEMIES = 0,
  ALLIES,
}

export enum CombatCardType {
  MARTIAL = "MARTIAL",
  MAGIC = "MAGIC",
}

export enum CombatCardId {
  SLICE = "SLICE",
  STAB = "STAB",
  DEFEND = "DEFEND",
}
